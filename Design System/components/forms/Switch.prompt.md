**Switch** — a calm on/off toggle for Steady's handful of settings, with optional label + description.

```jsx
<Switch label="Bedtime theme" description="Dim, warm colors after dark"
        checked={dark} onChange={setDark} />
```

The knob slides on a slow settle easing. Wrap label + description on the left; the toggle sits right.
