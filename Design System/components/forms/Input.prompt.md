**Input** — a calm text field with label above and an optional helper/trust line below.

```jsx
<Input label="Email" type="email" placeholder="you@example.com"
       helper="We only use this to save your progress." />
```

56px tall, soft green focus ring. Use `helper` to carry the plain one-sentence trust line. `invalid` turns border + helper to the muted crisis red.
